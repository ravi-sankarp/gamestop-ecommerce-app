import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#191b13'
    },
    secondary: {
      main: '#ffffff'
    }
  }
});
export default theme;
